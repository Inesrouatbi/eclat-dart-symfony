<form method="post" action="{{ path('app_oeuvres_delete', {'id': oeuvre.idoeuvre}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ oeuvre.idoeuvre) }}">
    <button class="btn">Delete</button>
</form>
